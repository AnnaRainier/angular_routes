import { ChangeFieldPipe } from './change-field.pipe';

describe('ChangeFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
